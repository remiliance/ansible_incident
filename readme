ansible-playbook  -i inventaire.ini playbook.yml
sudo ansible-playbook  -i inventaire.ini playbook.yml --vault-password-file=.vault_pass

Pour editer le vault ansible de logtash :
~/Documents/ansible/ansible_incident/roles/logstash/vars
=> ansible-vault view logstash-my-secrets.yml OU edit
(il faut avoir au préalable installer pip install ansible-vault avec aussi un venv)
